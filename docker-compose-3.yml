services:
  worker_a:
    build: .
    environment:
      BUCKET: proyecto-interdisciplinario-coil-grupo07
      RUN_ID: run_3
      WORKER_ID: worker_a
      MANIFEST_PATH: /app/manifests/run_3/worker_a.txt
      OUTPUT_PREFIX: curated/decisiones_cobertura
      AWS_REGION: us-east-1
      AWS_DEFAULT_REGION: us-east-1
    volumes:
      - ./manifests:/app/manifests
      - ~/.aws:/root/.aws:ro

  worker_b:
    build: .
    environment:
      BUCKET: proyecto-interdisciplinario-coil-grupo07
      RUN_ID: run_3
      WORKER_ID: worker_b
      MANIFEST_PATH: /app/manifests/run_3/worker_b.txt
      OUTPUT_PREFIX: curated/decisiones_cobertura
      AWS_REGION: us-east-1
      AWS_DEFAULT_REGION: us-east-1
    volumes:
      - ./manifests:/app/manifests
      - ~/.aws:/root/.aws:ro

  worker_c:
    build: .
    environment:
      BUCKET: proyecto-interdisciplinario-coil-grupo07
      RUN_ID: run_3
      WORKER_ID: worker_c
      MANIFEST_PATH: /app/manifests/run_3/worker_c.txt
      OUTPUT_PREFIX: curated/decisiones_cobertura
      AWS_REGION: us-east-1
      AWS_DEFAULT_REGION: us-east-1
    volumes:
      - ./manifests:/app/manifests
      - ~/.aws:/root/.aws:ro
